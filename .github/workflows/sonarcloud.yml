name: Java CI with Maven

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Maven Package
        run: mvn -B clean verify

      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        with:
            name: jacoco-reports
            path: |
              MS_Calculator/target/site/jacoco/
              MS_Deal/target/site/jacoco/
              MS_Dossier/target/site/jacoco/
              MS_Gateway/target/site/jacoco/
              MS_Statement/target/site/jacoco/
            if-no-files-found: warn

      - name: Upload aggregated coverage report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-aggregate-report
          path: target/site/jacoco-aggregate/
          if-no-files-found: warn

      - name: Run SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn sonar:sonar \
            -Dsonar.projectKey=robinx51_Calculator_Neoflex \
            -Dsonar.organization=robinx51 \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.scm.disabled=true
